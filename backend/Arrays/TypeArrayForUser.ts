const userStatus = ['Normal', 'Suspendned', 'Delete'];

const userRole = ['User', 'Admin'];

const userDeleteListDescription = ['Admin-Request Deletion', 'User-Request Deletion'];

export {userStatus, userRole, userDeleteListDescription}